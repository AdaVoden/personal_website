version: 1

pre-build: |
    echo Installing Bundler...
    gem install --no-document bundler
    echo Installing Jekyll and dependencies...
    gem install --no-document jekyll jekyll-seo-tag jekyll-sitemap jekyll-feed jekyll-archives jekyll-paginate